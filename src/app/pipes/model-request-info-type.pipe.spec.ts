import { ModelRequestInfoTypePipe } from './model-request-info-type.pipe';

describe('ModelRequestInfoTypePipe', () => {
    it('create an instance', () => {
        const pipe = new ModelRequestInfoTypePipe();
        expect(pipe).toBeTruthy();
    });
});
