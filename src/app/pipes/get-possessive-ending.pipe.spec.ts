import { GetPossessiveEndingPipe } from './get-possessive-ending.pipe';

describe('GetPossessiveEndingPipe', () => {
    it('create an instance', () => {
        const pipe = new GetPossessiveEndingPipe();
        expect(pipe).toBeTruthy();
    });
});
