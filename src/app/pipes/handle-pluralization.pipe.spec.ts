import { HandlePluralizationPipe } from './handle-pluralization.pipe';

describe('HandlePluralizationPipe', () => {
    it('create an instance', () => {
        const pipe = new HandlePluralizationPipe();
        expect(pipe).toBeTruthy();
    });
});
