import { CombineListElementsPipe } from './combine-list-elements.pipe';

describe('CombineListElementsPipe', () => {
    it('create an instance', () => {
        const pipe = new CombineListElementsPipe();
        expect(pipe).toBeTruthy();
    });
});
